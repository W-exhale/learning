一个文字接口，允许你执行执行程序，输入并获取某种半结构化的输出。

- 下面使用Bourne Again SHell，简称“bash”，这是最广泛使用的一种shell


在终端输入`date`，会显示当前时间
输入`echo hello`，会输出hello，shell是根据空格来拆分命令的，如果要在输出显示空格，可以用\\来代表空格 

- 在shell中导航
shell中的路径是一组被分割的目录，在linux和macOS上使用/分割，windows使用\\分割。

