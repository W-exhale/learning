- 关系型数据库：两张表的共有字段去确定数据的完整性

- 行：一条数据
- 列：一个字段，属性

每一个属性一般来说都会设计一个总数
- 数据冗余：一模一样的数据存储多余一份的情况。
-冗余的优点：提高查询性能...

- 实体与实体之间的关系：一对一，一对多，多对一，多对多

- 六个范式（一般就三个）：
	1. 确保每一列的原子性：最小化，不能被分割了（例如年份区间某年到某年改为起始时间为某年，截止时间为某年）
	2. 表里的内容必须与表相符：不能出现学生表里出现与学生不相关的东西
	3. 消除传递依赖：删除没必要的字段