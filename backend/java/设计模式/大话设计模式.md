一个类可以支持多个接口，多个类也可以支持相同的接口

类是对对象的抽象，抽象类是对类的抽象。接口是对行为的抽象
如果行为跨越不同类的对象，可使用接口，对于一些相似的类对象，用继承抽象类。

Arraylist类
使用大小可按需动态增加的数组实现collection接口．collection接口定义了很多集合用的方法

Array list的容量可根据需要自动扩充，而数组的容量是固定的


Add (), remov（）

在remove前面的数据后，后面的数据会整体前移，在arraylist眼中所有元素都是object.